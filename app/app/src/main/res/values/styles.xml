<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="TransparentButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:padding">16dp</item>
        <item name="android:drawableBottom">@android:color/transparent</item>
        <item name="android:drawableLeft">@android:color/transparent</item>
        <item name="android:drawableRight">@android:color/transparent</item>
        <item name="android:drawableTop">@android:color/transparent</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:background">@drawable/bottom_outline</item>
    </style>
</resources>